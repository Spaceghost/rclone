diff --git a/cmd/serve/s3/serve_s3.md b/cmd/serve/s3/serve_s3.md
--- a/cmd/serve/s3/serve_s3.md
+++ b/cmd/serve/s3/serve_s3.md
@@ -124,9 +124,10 @@ writes to the destination:
   object without comparing its contents.
 
-A failed condition returns `412 PreconditionFailed`, except that `If-Match`
-on a missing object returns `404 NoSuchKey`. Only a single strong ETag is
-accepted; empty conditions, weak ETags and ETag lists return `400 InvalidArgument`.
+A condition which is false when evaluated returns `412 PreconditionFailed`.
+If the destination changes after that evaluation while an atomic local commit
+is being published, the request returns `409 ConditionalRequestConflict`.
+Empty conditions, weak ETags and ETag lists return `400 InvalidArgument`.
 For multipart uploads the condition is checked at completion, not initiation.
 A rejected completion leaves the upload available to retry or abort.
@@ -141,12 +142,16 @@ must not alias one backing object.
 
 On Linux with a local XFS backend and VFS caching below `minimal`, conditional
 plain PUT commits use `XFS_IOC_START_COMMIT` and `XFS_IOC_COMMIT_RANGE` when
-the running kernel supports them. XFS rechecks destination freshness at the
-atomic range exchange, so a destination changed outside this server causes
-the commit to fail instead of silently overwriting the newer contents. Other
-filesystems, older kernels and cached VFS writes use the normal serialized
-rename path.
+replacing an existing object and the running kernel supports them. XFS checks
+destination freshness at the atomic range exchange, so a destination changed
+outside this server causes a `409 ConditionalRequestConflict` instead of
+silently overwriting the newer contents. `If-None-Match: *` on the local Linux
+backend uses `renameat2(RENAME_NOREPLACE)` when available so creation remains
+atomic against writers outside this server. Other filesystems, older kernels
+and cached VFS writes retain the normal in-server serialization and rename
+path.
