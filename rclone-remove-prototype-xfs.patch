diff --git a/cmd/serve/s3/backend.go b/cmd/serve/s3/backend.go
--- a/cmd/serve/s3/backend.go
+++ b/cmd/serve/s3/backend.go
@@ -436,18 +436,9 @@ func (b *s3Backend) PutObjectWithConditions(ctx context.Context, bucketName, obj
 
 	// Rename the temporary object into place
 	if tmpFp != fp {
-		if conditions != nil {
-			used, err := tryXFSCommit(_vfs, tmpFp, fp, n)
-			if err != nil {
-				cleanup()
-				return result, err
-			}
-			if used {
-				b.forgetPath(_vfs, tmpFp)
-				tmpFp = fp
-			}
-		}
-		if tmpFp != fp {
-			if err := _vfs.Rename(tmpFp, fp); err != nil {
-				cleanup()
-				return result, err
-			}
+		if err := _vfs.Rename(tmpFp, fp); err != nil {
+			cleanup()
+			return result, err
 		}
 	}
